# Optional legacy API

The mobile demo app uses **in-memory / localStorage mock data** (`src/lib/mockStore.ts`).
You do **not** need to run this server for the demo.

Start only if you want to experiment with a simple FastAPI + SQLite prototype:

```bash
npm run backend:install
npm run backend
```
